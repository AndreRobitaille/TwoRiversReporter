# Kamal secrets — pulled from local files and CLI tools, never raw credentials.

RAILS_MASTER_KEY=$(cat config/master.key)
KAMAL_REGISTRY_PASSWORD=$(gh auth token)
TWO_RIVERS_REPORTER_DATABASE_PASSWORD=$TWO_RIVERS_REPORTER_DATABASE_PASSWORD
POSTGRES_PASSWORD=$TWO_RIVERS_REPORTER_DATABASE_PASSWORD
GA_MEASUREMENT_ID=$GA_MEASUREMENT_ID
